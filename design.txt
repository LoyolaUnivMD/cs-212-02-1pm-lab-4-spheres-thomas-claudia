Design:
Class name: Sphere
Radius(1):
points(3):
area:
volume:
distance:

Area:
return 4 * Math.pi * Math.pow(radius, 2)

Volume:
return 4/3 * Math.pi * Math.pow(radius, 3)

Distance(Sphere sphere2)
return Math.sqrt(Math.pow(x - sphere2.x, 2) + Math.pow(y - sphere2.y, 2) + Math.pow(z - sphere2.z , 2))
